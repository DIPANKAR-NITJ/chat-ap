import React, { use, useState } from 'react'
import Chatcontainer from '../compenents/Chatcontainer'
import Rightsidebar from '../compenents/Rightsidebar'   
import Sidebar from '../compenents/Sidebar'

const Home = () => {
  const [selecteduser,setselecteduser]=useState(false)
  return (
    <div className='border w-full h-screen  sm:px[15%] sm:py[5%]'>
      <div className ={`backdrop-blur x1:border 2 border-grey-600 rounded-2xl
      overflow-hidden h-[100%] grid grid-cols 1 relative ${selecteduser ? 'md:grid-cols-[1fr_1.5fr_1fr] x1:grid-cols-[1fr_2fr_1fr]': 'md:grid-cols-2'}
      `}>
      <Chatcontainer />
      <Rightsidebar />
      <Sidebar />
      </div>
    </div>
  )
}

export default Home
